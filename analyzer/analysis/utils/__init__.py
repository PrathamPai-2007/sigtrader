"""Shared utilities used across the analysis package.

  indicators_compute.py  — compute_all_indicators and low-level indicator helpers
  concurrency.py         — ParallelAnalyzer, QueryOptimizer for parallel scanning
  replay.py              — find_latest_tradable_chart_timestamp for backtesting
  validation.py          — CandleValidator and DataQualityMonitor
  scoring/utils.py       — _clamp, _quantize, _quality_label, _quality_score_cap_from_confidence
"""
